# Next task

Resume only Phase 105 after the caller supplies an immutable local MTGJSON AllPrintings JSON
artifact and expected SHA-256 as specified in `TARGETED_MTGJSON_SET_POPULATION.md`. Begin with
validation and name discovery; do not assume set codes. Until then, add no generic importer,
placeholder, fixture-derived production data, or canonical entity. Architecture v12 must remain
unchanged and all promotion must remain independently reviewed.
